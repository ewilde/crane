using FakeItEasy;
using Xbehave;
using Xunit.Extensions;

namespace Crane.Console.Tests
{

    public class ProgramTests
    {
        //[Scenario]
        //public void RunningProgram_no_arguments(MockContainer<Program> program, string[] args)
        //{
        //    "Given I start the crane console"
        //        ._(() => program = B.AutoMock<Program>());

        //    "And I supply no agruments"
        //        ._(() => args = new string[]{});

        //    "When I run the program"
        //        ._(() => program.Subject.Run());

        //    "Then is should show the help text"
        //        ._(() => A.CallTo(() => program.GetMock<IOptions>().ShowHelp()).MustHaveHappened());
        //}
    }
}